这是一个非常经典的\*\*表单引擎（Form Engine）**或是**低代码（Low-Code）\*\*场景。在 Vue 3 中，利用 `component :is` 动态组件、`v-bind` 批量绑定属性以及 `v-on` 动态绑定事件，可以非常优雅地实现这一需求。

针对你的需求（输入框、单选、下拉 + 代码写死的事件 + 数据驱动），我为你设计了一套完整的方案。

-----

### 1\. 数据结构设计 (Schema Design)

我们需要定义一份 JSON 结构的配置（Schema），它不仅描述表单长什么样，还要描述它“行为”的索引（即事件名称）。

```typescript
// 这里的 TypeScript 接口仅用于说明结构，实际使用可以是纯 JSON 对象
interface FormItemSchema {
  // 组件类型：映射到具体的组件 ('input', 'select', 'radio')
  type: 'input' | 'select' | 'radio';
  
  // 字段名：对应表单数据模型中的 key
  field: string;
  
  // 标签名
  label: string;
  
  // 组件的原生 props 或自定义 props (如 placeholder, disabled, options)
  props?: {
    placeholder?: string;
    disabled?: boolean;
    // 针对 select 和 radio 的选项数据
    options?: Array<{ label: string; value: string | number }>;
    [key: string]: any;
  };

  // 事件映射：Key 是 DOM/组件事件名，Value 是我们在代码中写好的“函数名字符串”
  // 例如: { "change": "handleCityChange", "blur": "validateName" }
  eventMap?: Record<string, string>;
}
```

#### JSON 数据示例

```javascript
const formSchema = [
  {
    type: 'input',
    field: 'username',
    label: '用户名',
    props: { placeholder: '请输入用户名', clearable: true },
    // 绑定 input 的 blur 事件 到代码中的 onValidateName 函数
    eventMap: { blur: 'onValidateName' }
  },
  {
    type: 'select',
    field: 'city',
    label: '城市',
    props: {
      placeholder: '请选择城市',
      options: [
        { label: '北京', value: 'bj' },
        { label: '上海', value: 'sh' }
      ]
    },
    // 绑定 change 事件 到代码中的 onCityChange 函数
    eventMap: { change: 'onCityChange' }
  },
  {
    type: 'radio',
    field: 'gender',
    label: '性别',
    props: {
      options: [
        { label: '男', value: 1 },
        { label: '女', value: 2 }
      ]
    },
    eventMap: { change: 'onGenderChange' }
  }
];
```

-----

### 2\. 核心架构设计

为了让逻辑清晰，我们需要一个**事件注册表（Event Registry）**。因为 JSON 数据里存不了函数引用，所以我们用“字符串 Key”在代码里查找对应的函数。

### 3\. Vue 3 代码实现

我们将实现一个通用的 `DynamicForm` 组件。

#### A. 准备组件映射

假设你使用的是 Element Plus 或 Ant Design Vue，也可以是原生 HTML。这里以 **Element Plus** 为例（逻辑通用于任何 UI 库）。

```javascript
// componentMap.js
import { ElInput, ElSelect, ElRadioGroup } from 'element-plus';

// 建立 字符串 -> 组件 的映射关系
export const COMPONENT_MAP = {
  input: ElInput,
  select: ElSelect,
  radio: ElRadioGroup // 注意：RadioGroup 内部通常需要循环渲染 Radio Button，稍后在模板特殊处理或封装一层
};
```

#### B. 主组件实现 (DynamicForm.vue)

```vue
<template>
  <el-form :model="formData" label-width="100px">
    <el-form-item
      v-for="item in schema"
      :key="item.field"
      :label="item.label"
    >
      <component
        :is="getComponent(item.type)"
        v-model="formData[item.field]"
        v-bind="item.props"
        v-on="resolveEvents(item.eventMap, item)"
      >
        <template v-if="item.type === 'select'">
          <el-option
            v-for="opt in item.props?.options"
            :key="opt.value"
            :label="opt.label"
            :value="opt.value"
          />
        </template>
        
        <template v-if="item.type === 'radio'">
          <el-radio
            v-for="opt in item.props?.options"
            :key="opt.value"
            :label="opt.value"
          >
            {{ opt.label }}
          </el-radio>
        </template>
      </component>
    </el-form-item>
  </el-form>
</template>

<script setup>
import { computed } from 'vue';
import { COMPONENT_MAP } from './componentMap';
import { ElOption, ElRadio } from 'element-plus';

const props = defineProps({
  schema: { type: Array, required: true }, // 表单配置
  modelValue: { type: Object, required: true }, // 表单数据对象
  eventRegistry: { type: Object, default: () => ({}) } // 硬编码的事件集合
});

const emit = defineEmits(['update:modelValue']);

// 使用 computed 代理 formData，实现双向绑定
const formData = computed({
  get: () => props.modelValue,
  set: (val) => emit('update:modelValue', val)
});

// 获取组件引用
const getComponent = (type) => {
  return COMPONENT_MAP[type] || 'div'; // 默认回退
};

/**
 * 核心：事件解析器
 * @param {Object} eventMapSchema JSON配置里的事件映射 { change: 'handleCityChange' }
 * @param {Object} item 当前字段的完整配置（可选，用于透传给事件处理函数）
 * @returns {Object} Vue 可识别的事件对象 { onChange: Fn, onBlur: Fn }
 */
const resolveEvents = (eventMapSchema, item) => {
  if (!eventMapSchema) return {};

  const resolvedEvents = {};

  // 遍历配置中的事件名 (如 'change', 'blur')
  Object.keys(eventMapSchema).forEach((eventName) => {
    // 获取硬编码的方法名 (如 'onCityChange')
    const handlerName = eventMapSchema[eventName];
    // 从传入的 eventRegistry 中找到实际函数
    const handlerFn = props.eventRegistry[handlerName];

    if (handlerFn && typeof handlerFn === 'function') {
      // 绑定函数，并额外注入上下文（如当前修改的值、字段配置等）
      resolvedEvents[eventName] = (...args) => {
        // args 是组件原本抛出的参数 (e.g., event object 或 value)
        // 我们也可以把 item 本身传回去，方便处理函数知道是哪个字段触发的
        handlerFn({ args, fieldConfig: item, formData: formData.value });
      };
    }
  });

  return resolvedEvents;
};
</script>
```

#### C. 父组件调用 (使用写死的事件)

这里展示如何在父组件中定义“写死”的事件，并传递给动态表单。

```vue
<template>
  <div class="page-container">
    <h2>动态表单演示</h2>
    <DynamicForm
      v-model="formModel"
      :schema="formJson"
      :event-registry="hardcodedActions"
    />
    
    <pre>{{ formModel }}</pre>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue';
import DynamicForm from './DynamicForm.vue';

// 1. 表单数据模型
const formModel = ref({
  username: '',
  city: '',
  gender: 1
});

// 2. 表单配置 (通常来自后端 API)
const formJson = [
  {
    type: 'input',
    field: 'username',
    label: '用户名',
    props: { placeholder: '输入姓名' },
    eventMap: { blur: 'handleUserBlur' } // 关联 handleUserBlur
  },
  {
    type: 'select',
    field: 'city',
    label: '城市',
    props: {
      options: [
        { label: '北京', value: 'bj' },
        { label: '上海', value: 'sh' }
      ]
    },
    eventMap: { change: 'handleCityChange' } // 关联 handleCityChange
  }
];

// 3. 硬编码的业务逻辑 (Action Registry)
const hardcodedActions = {
  handleUserBlur: ({ args, fieldConfig }) => {
    console.log(`校验字段: ${fieldConfig.field}`, args);
    // 这里可以写具体的业务逻辑，比如调用接口校验重名
  },
  
  handleCityChange: ({ args, formData }) => {
    const [newValue] = args;
    console.log(`城市变为: ${newValue}`);
    
    // 联动逻辑示例：如果选了上海，自动修改用户名
    if (newValue === 'sh') {
      formData.username = '上海用户';
      console.log('触发联动逻辑');
    }
  }
};
</script>
```

-----

### 4\. 方案亮点与关键点总结

1.  **`v-on="object"` 的妙用**:

      * Vue 3 支持直接将对象传给 `v-on`。
      * 我们在 `resolveEvents` 函数中，将 JSON 里的 `{ change: "methodName" }` 转换成了 `{ change: function() { ... } }`，实现了配置与执行的动态绑定。

2.  **解耦**:

      * **UI (Schema)** 负责展示。
      * **Logic (Registry)** 负责业务。
      * 后端只需要返回 JSON 字符串，前端通过 Registry 字典来接管逻辑，安全性高（避免了 `eval` 执行后端传来的代码）。

3.  **扩展性**:

      * 如果需要支持新的组件（比如 Switch, DatePicker），只需要在 `COMPONENT_MAP` 中注册，并在 Schema 中增加 `type` 即可，核心渲染逻辑不需要改动。

4.  **插槽处理 (Slots)**:

      * 上面的代码中，针对 `select` 和 `radio` 的子元素（options），我使用了 `v-if="type === ..."` 在模板中硬编码处理。
      * **更高级的做法**：如果子组件结构非常复杂，可以将子组件（Option, RadioButton）也做成 Schema 的一部分，通过递归组件的方式来渲染 `children` 数组。

### 接下来我可以为你做的是

你需要我针对 **“表单联动”**（例如：选择A选项，显示B输入框）或者 **“表单验证规则集成”**（将 rules 也放入 JSON Schema）提供进一步的代码扩展吗？